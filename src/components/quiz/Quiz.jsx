import React from 'react';
import { QuizContainer } from './QuizStyles';

const Quiz = () => {
   return (
      <QuizContainer id='projects'>
         <></>
      </QuizContainer>
   );
};

export default Quiz;
